# Database

This folder documents database assets for D-Indexer-Backend.

The executable migration currently lives in `src/db/migrate.ts` because it uses the application database client and TypeScript runtime.

Use this folder for:

- schema documentation
- future raw SQL migrations
- seed data that is safe to commit
- database maintenance notes

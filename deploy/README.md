# Deployment

Deployment-specific assets live here.

Current status:

- Dockerfile added for container builds
- Compose file added for local infrastructure
- Kubernetes manifests are documented placeholders until deployment targets are finalized

Always validate environment variables before deploying. See `docs/configuration.md`.

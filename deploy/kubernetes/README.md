# Kubernetes

This folder is reserved for Kubernetes manifests.

Before adding manifests, decide:

- namespace strategy
- secret management
- ingress controller
- healthcheck path and timeout
- migration execution strategy
- resource requests and limits

# Observability Checklist

- include request IDs in API responses
- log indexer failures with context
- log startup and shutdown events
- avoid logging secrets
- add metrics before relying on production SLOs
- document new operational signals

# Playbook: Add an Environment Variable

1. add validation in `src/config/env.ts`
2. update `.env.example`
3. update configuration docs
4. add tests for required or risky values
5. update deployment settings

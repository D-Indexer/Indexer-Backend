# Playbook: Debug Uploads

1. confirm request uses multipart field `file`
2. confirm file size and MIME type
3. check `IPFS_API_URL`
4. verify Kubo is reachable
5. inspect API error response request ID

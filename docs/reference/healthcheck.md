# Healthcheck Reference

`GET /health` verifies database reachability and reports the indexer cursor.

Use it for load balancer checks only when database reachability is required for serving traffic.

# IPFS Runbook

## Upload failures

Check that the Kubo RPC endpoint configured by `IPFS_API_URL` is reachable from the backend process.

## Gateway failures

Check `IPFS_GATEWAY` and verify the CID exists through the configured gateway.

# Service Boundaries

D-Indexer-Backend owns API reads, upload pinning, and event indexing.

It does not own contract execution, wallet signing, frontend rendering, or public IPFS gateway availability.

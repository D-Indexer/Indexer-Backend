# Alerts

Suggested alerts:

- `/health` returns non-200 for longer than the deployment tolerance
- indexer cursor does not advance while events are expected
- repeated Soroban RPC failures
- repeated PostgreSQL connection failures
- upload failure rate exceeds baseline

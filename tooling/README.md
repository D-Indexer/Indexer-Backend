# Tooling

Tooling configuration and notes live here.

Current root-level tooling:

- `tsconfig.json`
- `jest.config.json`
- `.github/workflows/ci.yml`

Keep tool-specific generated output out of git unless it is intentionally versioned.
